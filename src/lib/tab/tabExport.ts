// The tab staff PDF export shares the SVG exporter (jspdf + svg2pdf.js, A4 by
// default, auto orientation). Re-exported here so tab imports read cleanly.
export { downloadPdfFromContainer } from "@/lib/scaleExport";
